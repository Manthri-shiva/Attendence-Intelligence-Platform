# Database Migrations Versions Directory

This directory contains versioned migration files generated by Alembic:
- Run `alembic revision --autogenerate -m "description"` to generate a new migration file.
- Run `alembic upgrade head` to apply all migrations to the PostgreSQL database.
