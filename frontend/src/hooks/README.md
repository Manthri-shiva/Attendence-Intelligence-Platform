# Hooks Directory

This directory contains reusable React hooks:
- `useGeoTracking.ts` — Location retrieval, watchPosition bindings, and accuracy calculations.
- `useCamera.ts` — Controls webcam streams, checking browser capabilities, and photo capture hooks.
