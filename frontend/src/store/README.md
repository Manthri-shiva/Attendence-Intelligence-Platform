# Store Directory

This directory contains global Zustand state stores:
- `useAuthStore.ts` — Authentication state, current user context, and token persistence.
- `useThemeStore.ts` — Interface theme selections (light/dark/system).
